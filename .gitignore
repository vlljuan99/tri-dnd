node_modules/
dist/
*.log
.env

# Artefactos de las pruebas E2E (Playwright)
/test-results/
/playwright-report/
/e2e-results/

# Base de datos local y secretos generados
server/data/*.db
server/data/*.db-journal
server/data/*.db-wal
server/data/*.db-shm
server/data/jwt-secret.txt

# Subidas de usuarios (avatares, mapas)
server/data/uploads/

# Imágenes privadas del archivo narrativo del DM
server/data/narrative-media/

# Historial local de copias del archivo narrativo
server/data/backups/

# Estado operativo que solo existe en /opt/tridnd al desplegar
deploy/.deploy*.env
deploy/.last-backup
deploy/backups/
deploy/src/
deploy/src.tar.gz
