{
  "name": "tri-dnd",
  "version": "0.1.0",
  "private": true,
  "description": "Mesa de juego virtual para D&D 5e — jugar online y en persona con el grupo",
  "scripts": {
    "dev": "concurrently -n servidor,cliente -c yellow,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "sync-srd": "npm run sync-srd --prefix server",
    "test": "npm test --prefix server && npm test --prefix client",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "concurrently": "^9.1.2",
    "socket.io-client": "^4.8.1"
  }
}
